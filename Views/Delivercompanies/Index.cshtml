@model IEnumerable<OnlineStore.Models.Delivercompany>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Delivercompanyname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Delivercompanyaddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Delivercompanyname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Delivercompanyaddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Delivercompanyid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Delivercompanyid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Delivercompanyid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
